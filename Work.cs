namespace Dythco
{
    public class Work 
    {

public void Main(string argument) {
    //Dictionary<string, string> data = getData(Me.CustomData); 
    /*
    Dictionary<string, string> data = parseData(Me.CustomData); 
    IMyTextPanel panel = GridTerminalSystem.GetBlockWithName(data["refOutput"]) as IMyTextPanel; 
 
    string text = "";

    // Setup batteries
    text += getBatteryState(data);

    // Setup panels
    text += getPanelState();

    // Setup reactors
    //text += getReactorState();

    panel.WritePublicText(text, false);
    */
}

    }
}